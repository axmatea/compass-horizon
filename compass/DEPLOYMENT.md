# COMPASS Railway deployment

Public URL: https://compass-web-production-da39.up.railway.app
Project: compass-nayl-vincent
Project ID: d7a3caa9-648a-4162-a34d-3e3bdc6724cc
Service: compass-web
Service ID: 2f7f6c4e-7030-47c2-b391-5a0df7eaaa90
Workspace: AI’s Projects
Environment: production
Last recorded published commit before this local revision: 2a345e94fb99aeda74867753bdef2cda6c544edc
Last recorded deployment: 6c67e296-bc6b-4f41-a159-e143d229f404
These are historical references; verify Railway before any future release.

## Update

The `codex/compass-homepage` revision is local only. Do not deploy it until the
user requests publication. A push or merge to `main` automatically deploys.

1. Run `npm run build`, then `PORT=8780 npm start` for a local production build.
2. Inspect `/`, `/present.html`, `/#present`, the demo and the film. Follow the
   local QA record in `docs/HOMEPAGE_QA.md`.
3. When publication is requested, review the exact diff, stage only owned files,
   and use the existing GitHub repository and Railway service. No force push.
4. After publishing, verify Railway reports the intended commit, then inspect
   the anonymous public page, presentation and assets in the browser.

The Docker image includes the built `dist` folder and minimal Node server.
Vite builds both HTML entries; Docker must copy `present.html` before building.
No voice provider credentials are present. No new service or paid media is needed.

## Previous production verification, September 17, 2026 Pacific

- Anonymous HTTPS: main page, CSS, JavaScript modules, portrait, demo and health endpoint return 200.
- Internal server source and .env URLs return 404.
- Browser: desktop 1280×720 and mobile 390×844 reviewed; mobile page width 390px without horizontal overflow.
- Orb, correction movement, typed draft, keyboard End navigation and speaker notes exercised.
- Public greeting reached Speaking via browser speech synthesis. Audio quality was not separately auditioned.
- Public client correction updates Friday to confirmed and payment to the concern.
- Browser error log during local interaction was empty.
- Reduced-motion support reviewed in source; no OS-level reduced-motion test performed.

## Scope

Finished: deployed editable presentation, scripted demo and server.
Simulated: prepared conversations and context changes; greeting is browser TTS.
Not connected: live voice AI, persistent memory, external actions and latency measurements.
The previous film was not regenerated by this deployment. No DaVinci timeline was changed.

## GitHub deployment

Private repository: https://github.com/axmatea/compass
Railway service source: axmatea/compass, main, connected successfully. Pushes/merges to main build the Dockerfile and deploy. The browser artifact is built from editable src and public files, not a mirrored remote URL.

Previous handoff verified `victorfaren` with GitHub Write and the collaborator account as Railway Editor. Recheck current membership only when access changes are needed.

Imported Higgsfield React source remains preserved, but is not the active website entry. The active sci-fi presentation is now a separate entry. See MIGRATION.md for historical provenance and docs/PRODUCT_BRIEF.md for the current direction.
